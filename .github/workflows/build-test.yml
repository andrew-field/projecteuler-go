name: Build and test

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build_test:
    uses: andrew-field/reusable-workflows/.github/workflows/build-test.yml@master
