name: Super Linter

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

  # Allows you to run this workflow manually from the Actions tab.
  workflow_dispatch:

# Permissions for the GITHUB_TOKEN.
permissions: {}

jobs:
  lint:
    name: Lint code base
    permissions:
      contents: read # The contents: read is not necessary for a public repository but it is necessary for the reusable workflow, which has it as a requirement.
      packages: read
      issues: write
      pull-requests: write
      statuses: write
    uses: andrew-field/reusable-workflows/.github/workflows/super-linter.yml@d3e8af3799e0560bd8bbeeda7660216057c79528 # v1.5.24
    with:
      regex_exclude: p022_names.txt # Exclude this text file because there are no spelling mistakes.
