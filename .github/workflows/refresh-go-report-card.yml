name: Refresh go report card

on:
  push:
    branches: [master]

  # Allows you to run this workflow manually from the Actions tab.
  workflow_dispatch:

# Permissions for the GITHUB_TOKEN.
permissions: {}

jobs:
  post-request:
    name: Call refresh go report card action
    uses: andrew-field/reusable-workflows/.github/workflows/refresh-go-report-card.yml@7b87d70af7593c853b4afb3c69cb588a879e02f1 # v1.0.2
