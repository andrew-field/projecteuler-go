name: Refresh go report card

on:
  push:
    branches: [master]

  # Allows you to run this workflow manually from the Actions tab.
  workflow_dispatch:

# Permissions for the GITHUB_TOKEN.
permissions: {}

jobs:
  post-request:
    name: Call refresh go report card action
    uses: andrew-field/reusable-workflows/.github/workflows/refresh-go-report-card.yml@6d68839aeb4c6777dcb036f4688257e9b838f9cd # v1.5.2
